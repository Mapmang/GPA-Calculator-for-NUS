{
  "name": "nus-gpa-calculator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch-data": "node scripts/fetch-modules.mjs",
    "dev": "wrangler dev",
    "deploy": "npm run fetch-data && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
